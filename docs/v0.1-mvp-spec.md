# CoDesign Agent v0.1 MVP — 最小可运行版本规格

> **产品定位：** 这不是"开题报告生成器"，而是一个通过苏格拉底式对话进行设计思维训练、并把认知动作可视化的 iOS 智能 Agent。
>
> **v0.1 目标：** 用最小代码量跑通核心闭环——用户输入模糊创意 → AI 苏格拉底式追问 → 后台结构化提取 → 9 步进度点亮 → 反思卡片出现 → 设计思维可视化。
>
> **推荐产品名称：** **Clarify**（从模糊到清晰的认知训练过程）

---

## 一、v0.1 核心闭环

```
用户输入模糊创意（"我想做一个校园导航 App"）
    │
    ▼
MockLLMService 返回苏格拉底式追问
    "你觉得谁最需要这个？能描述一个具体场景吗？"
    │
    ▼
MockStructuredExtractor 从用户回答中提取关键字段
    → targetUser = "大一新生"
    → painPoint = "找不到教室"
    │
    ▼
ProgressAnalyzer 检查字段填充情况
    → 阶段 1（痛点与场景锚定）completionRatio: 0.67 → active
    │
    ▼
LearningTrace 检测认知动作
    → 用户修改了 targetUser → actionType: "reframe"
    → ReflectionCard 插入对话流："你重新定义了问题"
    │
    ▼
InsightsPanel 实时展示
    → DesignBrief 摘要（已填充 / 未填充字段）
    → 学习轨迹时间线
```

---

## 二、v0.1 范围边界

### ✅ 保留

| 模块 | 范围 |
|------|------|
| SwiftData 模型 | Project, ChatMessage, DesignBrief, ProgressStage, BoundaryItem, RiskItem, SuccessMetric, LearningTrace |
| DTO 层 | 全部保留（轻量 struct，开发成本低） |
| Mock 服务 | MockLLMService + MockStructuredExtractor（完整实现） |
| Live 服务 | LiveLLMService + LiveStructuredExtractor（接口骨架，内部降级到 Mock） |
| StageDefinition | 9 阶段 ↔ 字段映射（完整） |
| ProgressAnalyzer | 阶段状态计算 + 缺失字段 + 学习轨迹检测（v0.1 不定义 Protocol，直接用 struct） |
| 对话面板 | ChatPanel + MessageBubble + TypingIndicator |
| 进度面板 | ProgressPanel + StageNodeView |
| 洞察面板 | InsightsPanel（DesignBrief 摘要 + 学习轨迹时间线） |
| 反思卡片 | ReflectionCard（3 种核心认知动作：reframe, converge, boundaryShrink） |
| 种子数据 | MockDataFactory + SeedDataFactory |
| 主题 | Color+Theme |
| 服务注入 | 通过 SwiftUI Environment 注入，需定义 EnvironmentKey（见第六节） |

### ❌ 延后至 v0.2+

| 模块 | 延后原因 |
|------|---------|
| conversationSummary | v0.1 对话轮次少，直接传全部消息即可 |
| 完整错误处理矩阵 + ErrorBanner | v0.1 用 Mock 为主，用简单 Alert 代替 |
| ReportExporter + ExportPanel | 导出是副产物，不是核心价值 |
| RiskMatrixView 精细视图 | v0.1 在 InsightsPanel 中用简单列表代替 |
| BoundaryTableView 独立组件 | v0.1 在 InsightsPanel 中内联渲染 |
| StageProgressBar 独立组件 | v0.1 在 ProgressPanel 中内联布局 |
| MissingFieldsCard 独立组件 | v0.1 在 ProgressPanel 中内联渲染 |
| ModelActor 并发优化 | v0.1 数据量小，主线程足够 |
| LiveStructuredExtractor 完整实现 | v0.1 用 Mock 提取器，Live 留骨架 |
| EmptyStateView | v0.1 用 SwiftUI 原生空状态 |
| View+Animations / Date+Formatting | v0.1 用 SwiftUI 原生动画和日期格式 |
| LearningTrace 完整 7 种 actionType | v0.1 只做 3 种核心类型 |
| PromptTemplates | v0.1 Mock 模式不需要 Prompt；Live 骨架暂内置空字符串占位，v0.2 抽出独立文件 |

---

## 三、v0.1 文件结构

```
CoDesign-Agent/
├── CoDesign_AgentApp.swift                        # App 入口 + ModelContainer + 服务注入
│
├── Models/                                        # ── SwiftData 持久化模型 ──
│   ├── Project.swift                              # 项目主模型
│   ├── ChatMessage.swift                          # 聊天消息
│   ├── DesignBrief.swift                          # 结构化设计简报
│   ├── ProgressStage.swift                        # 9 步流程节点
│   ├── BoundaryItem.swift                         # 项目边界表条目
│   ├── RiskItem.swift                             # 风险矩阵条目
│   ├── SuccessMetric.swift                        # 量化验收指标
│   ├── LearningTrace.swift                        # 设计思维学习轨迹
│   └── ModelExtensions.swift                      # Model ↔ DTO 转换 + applyExtracted
│
├── DTOs/                                          # ── 轻量传输对象 ──
│   ├── ChatPayloadMessage.swift                   # 对话消息 DTO + 工厂方法
│   ├── DesignBriefSnapshot.swift                  # 设计简报快照
│   ├── ProgressStageSnapshot.swift                # 进度节点快照 + StageStatus enum
│   ├── BoundaryItemDTO.swift                      # 边界条目 DTO
│   ├── RiskItemDTO.swift                          # 风险条目 DTO
│   ├── SuccessMetricDTO.swift                     # 验收指标 DTO
│   ├── ExtractedFields.swift                      # 结构化提取结果
│   └── LearningTraceDTO.swift                     # 学习轨迹 DTO
│
├── Services/                                      # ── 服务层 ──
│   ├── Protocols/
│   │   ├── LLMServiceProtocol.swift               # LLM 服务协议
│   │   └── StructuredExtractorProtocol.swift      # 结构化提取协议
│   ├── Mock/
│   │   ├── MockLLMService.swift                   # Mock LLM（预设苏格拉底追问）
│   │   └── MockStructuredExtractor.swift          # Mock 提取（关键词匹配）
│   ├── Live/
│   │   ├── LiveLLMService.swift                   # Live LLM 骨架（v0.1 降级到 Mock）
│   │   └── LiveStructuredExtractor.swift          # Live 提取骨架（v0.1 降级到 Mock）
│   ├── StageDefinition.swift                      # 9 阶段 ↔ 字段映射规则 + BriefField enum
│   └── ProgressAnalyzer.swift                     # 阶段状态 + 缺失字段 + 学习轨迹检测
│
├── Factories/
│   ├── MockDataFactory.swift                      # 生成完整 demo 项目
│   └── SeedDataFactory.swift                      # 首次启动种子数据
│
├── ViewModels/
│   ├── ProjectListViewModel.swift                 # 列表页逻辑
│   ├── NewProjectViewModel.swift                  # 新建项目逻辑
│   ├── ProjectDetailViewModel.swift               # 详情页 Tab 容器
│   └── ChatViewModel.swift                        # 对话核心逻辑
│
├── Views/
│   ├── ProjectList/
│   │   └── ProjectListView.swift                  # 列表页
│   ├── NewProject/
│   │   └── NewProjectView.swift                   # 新建项目页
│   ├── ProjectDetail/
│   │   ├── ProjectDetailView.swift                # 详情页（Tab 容器）
│   │   ├── ChatPanel.swift                        # 对话面板
│   │   ├── ProgressPanel.swift                    # 9 步进度面板
│   │   └── InsightsPanel.swift                    # 洞察面板
│   └── Components/
│       ├── MessageBubble.swift                    # 聊天气泡
│       ├── StageNodeView.swift                    # 阶段节点
│       ├── ReflectionCard.swift                   # 反思卡片
│       └── TypingIndicatorView.swift              # AI 输入动画
│
├── Extensions/
│   └── Color+Theme.swift                          # 颜色主题
│
└── Resources/
    └── Assets.xcassets
```

### v0.1 文件统计

| 目录 | 文件数 | 说明 |
|------|--------|------|
| 入口 | 1 | CoDesign_AgentApp.swift |
| Models/ | 9 | 8 个 @Model + ModelExtensions |
| DTOs/ | 8 | 纯 struct（含 ChatPayloadMessage 工厂方法） |
| Services/ | 8 | 2 协议 + 2 Mock + 2 Live 骨架 + StageDefinition + ProgressAnalyzer |
| Factories/ | 2 | MockDataFactory + SeedDataFactory |
| ViewModels/ | 4 | 无 InsightsViewModel（内联到 InsightsPanel） |
| Views/ | 8 | 4 个页面/面板 + 4 个组件 |
| Extensions/ | 1 | Color+Theme |
| **总计** | **42** | |

> **相对 v3 的变化：** v3 有 ~55 个文件（含 PromptTemplates, ConversationContextBuilder, ReportExporter, InsightsViewModel, ExportPanel, StageProgressBar, MissingFieldsCard, RiskMatrixView, BoundaryTableView, ErrorBanner, EmptyStateView, Date+Formatting, View+Animations）。v0.1 移除了 13 个文件，聚焦核心闭环。PromptTemplates 在 v0.1 不独立成文件，由 Live 骨架内置占位。

---

## 四、v0.1 数据模型

### 模型关系

```
Project (1) ──── (N) ChatMessage
Project (1) ──── (1) DesignBrief
Project (1) ──── (9) ProgressStage
Project (1) ──── (N) LearningTrace
DesignBrief (1) ── (N) BoundaryItem     ← 单一 relationship，isIncluded 区分
DesignBrief (1) ── (N) RiskItem
DesignBrief (1) ── (N) SuccessMetric
```

### v0.1 相对 v3 的差异

| 差异点 | v3 | v0.1 |
|--------|----|----|
| Project.conversationSummary | ✅ 有 | ❌ 无（v0.1 对话短，不需要摘要） |
| Project.lastSummaryAtMessageCount | ✅ 有 | ❌ 无 |
| Project init 参数 | 含 conversationSummary, lastSummaryAtMessageCount | 不含上述两字段，其余一致 |
| ProgressAnalyzer | 遵循 ProgressAnalyzerProtocol | 直接用 struct，不定义 Protocol |
| PromptTemplates | 独立文件 | 不独立，Live 骨架内置空字符串占位 |

### Project（v0.1 完整版）

```swift
import Foundation
import SwiftData

@Model
final class Project {
    @Attribute(.unique) var id: UUID
    var name: String
    var briefDescription: String
    var createdAt: Date
    var updatedAt: Date

    @Relationship(deleteRule: .cascade, inverse: \ChatMessage.project)
    var messages: [ChatMessage]

    @Relationship(deleteRule: .cascade, inverse: \DesignBrief.project)
    var brief: DesignBrief?

    @Relationship(deleteRule: .cascade, inverse: \ProgressStage.project)
    var stages: [ProgressStage]

    @Relationship(deleteRule: .cascade, inverse: \LearningTrace.project)
    var learningTraces: [LearningTrace]

    init(
        id: UUID = UUID(),
        name: String,
        briefDescription: String = "",
        createdAt: Date = Date(),
        updatedAt: Date = Date(),
        messages: [ChatMessage] = [],
        stages: [ProgressStage] = [],
        learningTraces: [LearningTrace] = []
    ) {
        self.id = id
        self.name = name
        self.briefDescription = briefDescription
        self.createdAt = createdAt
        self.updatedAt = updatedAt
        self.messages = messages
        self.stages = stages
        self.learningTraces = learningTraces
    }

    var completionRate: Double {
        guard !stages.isEmpty else { return 0 }
        let total = stages.reduce(0.0) { $0 + $1.completionRatio }
        return total / Double(stages.count)
    }
}
```

> **与 v3 的差异：** 去掉了 `conversationSummary: String?` 和 `lastSummaryAtMessageCount: Int`。init 相应简化。

### ChatMessage（与 v3 一致）

```swift
import Foundation
import SwiftData

@Model
final class ChatMessage {
    @Attribute(.unique) var id: UUID
    var role: String              // "user" | "assistant" | "system"
    var content: String
    var timestamp: Date
    var isStreaming: Bool

    var project: Project?         // 反向关系，SwiftData 自动维护

    init(
        id: UUID = UUID(),
        role: String,
        content: String,
        timestamp: Date = Date(),
        isStreaming: Bool = false
    ) {
        self.id = id
        self.role = role
        self.content = content
        self.timestamp = timestamp
        self.isStreaming = isStreaming
    }
}
```

### DesignBrief（与 v3 一致）

```swift
import Foundation
import SwiftData

@Model
final class DesignBrief {
    @Attribute(.unique) var id: UUID
    var lastExtractedAt: Date?

    // ── 阶段 1：痛点与场景锚定 ──
    var targetUser: String?
    var painPoint: String?
    var useScenario: String?

    // ── 阶段 2：差异化价值提炼 ──
    var coreValue: String?
    var differentiation: String?

    // ── 阶段 3：项目边界划定（单一 relationship，isIncluded 区分） ──
    @Relationship(deleteRule: .cascade, inverse: \BoundaryItem.brief)
    var boundaryItems: [BoundaryItem]

    // ── 阶段 4：功能与技术方案拆解 ──
    var mvpFeatures: String?
    var technicalModules: String?
    var interactionFlow: String?

    // ── 阶段 5：运行逻辑与规则定义 ──
    var operationLogic: String?

    // ── 阶段 6：硬性约束设计 ──
    var hardConstraints: String?

    // ── 阶段 7：量化验收标准 ──
    @Relationship(deleteRule: .cascade, inverse: \SuccessMetric.brief)
    var successMetrics: [SuccessMetric]

    // ── 阶段 8：风险识别与预案 ──
    @Relationship(deleteRule: .cascade, inverse: \RiskItem.brief)
    var risks: [RiskItem]

    // ── 阶段 9：项目阶段拆分与排期 ──
    var milestones: String?

    var project: Project?         // 反向关系，SwiftData 自动维护

    init(
        id: UUID = UUID(),
        lastExtractedAt: Date? = nil,
        targetUser: String? = nil,
        painPoint: String? = nil,
        useScenario: String? = nil,
        coreValue: String? = nil,
        differentiation: String? = nil,
        boundaryItems: [BoundaryItem] = [],
        mvpFeatures: String? = nil,
        technicalModules: String? = nil,
        interactionFlow: String? = nil,
        operationLogic: String? = nil,
        hardConstraints: String? = nil,
        successMetrics: [SuccessMetric] = [],
        risks: [RiskItem] = [],
        milestones: String? = nil
    ) {
        self.id = id
        self.lastExtractedAt = lastExtractedAt
        self.targetUser = targetUser
        self.painPoint = painPoint
        self.useScenario = useScenario
        self.coreValue = coreValue
        self.differentiation = differentiation
        self.boundaryItems = boundaryItems
        self.mvpFeatures = mvpFeatures
        self.technicalModules = technicalModules
        self.interactionFlow = interactionFlow
        self.operationLogic = operationLogic
        self.hardConstraints = hardConstraints
        self.successMetrics = successMetrics
        self.risks = risks
        self.milestones = milestones
    }

    // ── 便捷计算属性 ──
    var includedFeatures: [BoundaryItem] {
        boundaryItems.filter { $0.isIncluded }
    }
    var excludedFeatures: [BoundaryItem] {
        boundaryItems.filter { !$0.isIncluded }
    }
}
```

### ProgressStage（与 v3 一致）

```swift
import Foundation
import SwiftData

@Model
final class ProgressStage {
    @Attribute(.unique) var id: UUID
    var order: Int                // 1~9
    var name: String
    var status: String            // "notStarted" | "active" | "completed" | "needsReview"
    var completionRatio: Double   // 0.0 ~ 1.0
    var lastUpdated: Date?

    var project: Project?         // 反向关系，SwiftData 自动维护

    init(
        id: UUID = UUID(),
        order: Int,
        name: String,
        status: String = "notStarted",
        completionRatio: Double = 0.0,
        lastUpdated: Date? = nil
    ) {
        self.id = id
        self.order = order
        self.name = name
        self.status = status
        self.completionRatio = completionRatio
        self.lastUpdated = lastUpdated
    }

    /// Model(String) ↔ DTO(StageStatus) 桥接
    var stageStatusValue: StageStatus {
        get { StageStatus(rawValue: status) ?? .notStarted }
        set { status = newValue.rawValue }
    }
}
```

### BoundaryItem / RiskItem / SuccessMetric / LearningTrace（与 v3 一致）

```swift
// BoundaryItem
@Model
final class BoundaryItem {
    @Attribute(.unique) var id: UUID
    var content: String
    var isIncluded: Bool          // true = MVP 做, false = 明确排除
    var brief: DesignBrief?

    init(id: UUID = UUID(), content: String, isIncluded: Bool = true) {
        self.id = id; self.content = content; self.isIncluded = isIncluded
    }
}

// RiskItem
@Model
final class RiskItem {
    @Attribute(.unique) var id: UUID
    var description: String
    var probability: Int          // 1~5
    var impact: Int               // 1~5
    var mitigation: String?
    var brief: DesignBrief?

    init(id: UUID = UUID(), description: String,
         probability: Int = 3, impact: Int = 3, mitigation: String? = nil) {
        self.id = id; self.description = description
        self.probability = probability; self.impact = impact
        self.mitigation = mitigation
    }
}

// SuccessMetric
@Model
final class SuccessMetric {
    @Attribute(.unique) var id: UUID
    var metric: String
    var target: String
    var measurement: String?
    var brief: DesignBrief?

    init(id: UUID = UUID(), metric: String, target: String, measurement: String? = nil) {
        self.id = id; self.metric = metric
        self.target = target; self.measurement = measurement
    }
}

// LearningTrace
@Model
final class LearningTrace {
    @Attribute(.unique) var id: UUID
    var stageOrder: Int           // 关联阶段 1~9
    var actionType: String        // v0.1: "reframe" | "converge" | "boundaryShrink"
    var title: String
    var detail: String
    var timestamp: Date
    var project: Project?

    init(id: UUID = UUID(), stageOrder: Int, actionType: String,
         title: String, detail: String, timestamp: Date = Date()) {
        self.id = id; self.stageOrder = stageOrder
        self.actionType = actionType; self.title = title
        self.detail = detail; self.timestamp = timestamp
    }
}
```

### @Model 初始化核心约束

| 约束 | 说明 |
|------|------|
| 手写 init | 不依赖 SwiftData 自动合成，确保编译稳定 |
| UUID 默认值 | `id: UUID = UUID()` |
| Date 默认值 | `timestamp: Date = Date()` |
| 数组默认值 | `= []`（SwiftData 要求非 nil） |
| Optional 默认值 | `= nil` |
| 反向关系不出现在 init 中 | `project`、`brief` 由 SwiftData 在 insert/赋值时自动维护 |
| enum 用 String 存储 | `ProgressStage.status` 用 String，通过 `stageStatusValue` 桥接 StageStatus enum |

---

## 五、DTO / Snapshot 设计

### 1. ChatPayloadMessage（含工厂方法）

```swift
struct ChatPayloadMessage: Codable {
    let role: String              // "system" | "user" | "assistant"
    let content: String

    // 工厂方法，方便构造
    static func system(_ content: String) -> ChatPayloadMessage {
        ChatPayloadMessage(role: "system", content: content)
    }
    static func user(_ content: String) -> ChatPayloadMessage {
        ChatPayloadMessage(role: "user", content: content)
    }
    static func assistant(_ content: String) -> ChatPayloadMessage {
        ChatPayloadMessage(role: "assistant", content: content)
    }
}
```

### 2. DesignBriefSnapshot（含空 init）

```swift
struct DesignBriefSnapshot: Codable {
    var targetUser: String?
    var painPoint: String?
    var useScenario: String?
    var coreValue: String?
    var differentiation: String?
    var boundaryItems: [BoundaryItemDTO]
    var mvpFeatures: String?
    var technicalModules: String?
    var interactionFlow: String?
    var operationLogic: String?
    var hardConstraints: String?
    var successMetrics: [SuccessMetricDTO]
    var risks: [RiskItemDTO]
    var milestones: String?

    /// 空快照，用于对比基准
    init(
        targetUser: String? = nil,
        painPoint: String? = nil,
        useScenario: String? = nil,
        coreValue: String? = nil,
        differentiation: String? = nil,
        boundaryItems: [BoundaryItemDTO] = [],
        mvpFeatures: String? = nil,
        technicalModules: String? = nil,
        interactionFlow: String? = nil,
        operationLogic: String? = nil,
        hardConstraints: String? = nil,
        successMetrics: [SuccessMetricDTO] = [],
        risks: [RiskItemDTO] = [],
        milestones: String? = nil
    ) {
        self.targetUser = targetUser
        self.painPoint = painPoint
        self.useScenario = useScenario
        self.coreValue = coreValue
        self.differentiation = differentiation
        self.boundaryItems = boundaryItems
        self.mvpFeatures = mvpFeatures
        self.technicalModules = technicalModules
        self.interactionFlow = interactionFlow
        self.operationLogic = operationLogic
        self.hardConstraints = hardConstraints
        self.successMetrics = successMetrics
        self.risks = risks
        self.milestones = milestones
    }
}
```

> **v0.1 明确：** 必须提供显式 init 且所有参数有默认值。ChatViewModel 中 `DesignBriefSnapshot()` 表示空快照，用于首次对比。

### 3. ProgressStageSnapshot + StageStatus

```swift
struct ProgressStageSnapshot: Codable {
    let order: Int
    let name: String
    var status: StageStatus
    var completionRatio: Double
}

enum StageStatus: String, Codable {
    case notStarted
    case active
    case completed
    case needsReview
}
```

### 4. 子实体 DTO

```swift
struct BoundaryItemDTO: Codable, Identifiable {
    var id: UUID?
    var content: String
    var isIncluded: Bool
}

struct RiskItemDTO: Codable, Identifiable {
    var id: UUID?
    var description: String
    var probability: Int          // 1~5
    var impact: Int               // 1~5
    var mitigation: String?
}

struct SuccessMetricDTO: Codable, Identifiable {
    var id: UUID?
    var metric: String
    var target: String
    var measurement: String?
}
```

### 5. ExtractedFields

```swift
struct ExtractedFields: Codable {
    var targetUser: String?
    var painPoint: String?
    var useScenario: String?
    var coreValue: String?
    var differentiation: String?
    var boundaryItems: [BoundaryItemDTO]?
    var mvpFeatures: String?
    var technicalModules: String?
    var interactionFlow: String?
    var hardConstraints: String?
    var operationLogic: String?
    var successMetrics: [SuccessMetricDTO]?
    var risks: [RiskItemDTO]?
    var milestones: String?

    // 所有字段默认 nil，表示"本轮未提取到"
    init(
        targetUser: String? = nil,
        painPoint: String? = nil,
        useScenario: String? = nil,
        coreValue: String? = nil,
        differentiation: String? = nil,
        boundaryItems: [BoundaryItemDTO]? = nil,
        mvpFeatures: String? = nil,
        technicalModules: String? = nil,
        interactionFlow: String? = nil,
        hardConstraints: String? = nil,
        operationLogic: String? = nil,
        successMetrics: [SuccessMetricDTO]? = nil,
        risks: [RiskItemDTO]? = nil,
        milestones: String? = nil
    ) {
        self.targetUser = targetUser
        self.painPoint = painPoint
        self.useScenario = useScenario
        self.coreValue = coreValue
        self.differentiation = differentiation
        self.boundaryItems = boundaryItems
        self.mvpFeatures = mvpFeatures
        self.technicalModules = technicalModules
        self.interactionFlow = interactionFlow
        self.hardConstraints = hardConstraints
        self.operationLogic = operationLogic
        self.successMetrics = successMetrics
        self.risks = risks
        self.milestones = milestones
    }
}
```

> **v0.1 明确：** ExtractedFields 必须提供显式 init（所有参数 nil），否则 MockStructuredExtractor 中 `var fields = ExtractedFields()` 无法编译。

### 6. LearningTraceDTO

```swift
struct LearningTraceDTO: Codable, Identifiable {
    var id: UUID?
    var stageOrder: Int
    var actionType: String
    var title: String
    var detail: String
    var timestamp: Date?

    init(id: UUID? = nil, stageOrder: Int, actionType: String,
         title: String, detail: String, timestamp: Date? = nil) {
        self.id = id; self.stageOrder = stageOrder
        self.actionType = actionType; self.title = title
        self.detail = detail; self.timestamp = timestamp
    }
}
```

---

## 六、v0.1 服务注入机制

> **v0.1 关键补充：** 必须定义 SwiftUI EnvironmentKey 和扩展，否则 `.environment(\.llmService, ...)` 无法编译。

### EnvironmentKey 定义

```swift
// 建议放在 CoDesign_AgentApp.swift 中，或独立的 ServiceEnvironment.swift 中
// v0.1 暂放在 App 入口文件，不单独新建文件

import SwiftUI

// ── LLMService 环境注入 ──
private struct LLMServiceKey: EnvironmentKey {
    static let defaultValue: LLMServiceProtocol = MockLLMService()
}

extension EnvironmentValues {
    var llmService: LLMServiceProtocol {
        get { self[LLMServiceKey.self] }
        set { self[LLMServiceKey.self] = newValue }
    }
}

// ── StructuredExtractor 环境注入 ──
private struct StructuredExtractorKey: EnvironmentKey {
    static let defaultValue: StructuredExtractorProtocol = MockStructuredExtractor()
}

extension EnvironmentValues {
    var structuredExtractor: StructuredExtractorProtocol {
        get { self[StructuredExtractorKey.self] }
        set { self[StructuredExtractorKey.self] = newValue }
    }
}
```

### CoDesign_AgentApp 完整实现

```swift
import SwiftUI
import SwiftData

enum ServiceMode: String {
    case mock
    case live
}

@main
struct CoDesign_AgentApp: App {
    @AppStorage("serviceMode") private var serviceModeRaw: String = "mock"

    var sharedModelContainer: ModelContainer = {
        let schema = Schema([
            Project.self, ChatMessage.self, DesignBrief.self,
            ProgressStage.self, BoundaryItem.self, RiskItem.self,
            SuccessMetric.self, LearningTrace.self,
        ])
        let config = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)
        do {
            return try ModelContainer(for: schema, configurations: [config])
        } catch {
            fatalError("Could not create ModelContainer: \(error)")
        }
    }()

    var serviceMode: ServiceMode {
        ServiceMode(rawValue: serviceModeRaw) ?? .mock
    }

    var llmService: LLMServiceProtocol {
        serviceMode == .live ? LiveLLMService() : MockLLMService()
    }

    var extractor: StructuredExtractorProtocol {
        serviceMode == .live ? LiveStructuredExtractor() : MockStructuredExtractor()
    }

    var body: some Scene {
        WindowGroup {
            ProjectListView()
                .environment(\.llmService, llmService)
                .environment(\.structuredExtractor, extractor)
                .task {
                    SeedDataFactory.seedIfNeeded(
                        context: sharedModelContainer.mainContext
                    )
                }
        }
        .modelContainer(sharedModelContainer)
    }
}
```

> **注意：** `defaultValue` 设为 Mock 实现，确保即使忘记注入也不会崩溃。

---

## 七、v0.1 核心服务设计

### 1. Service Protocols

```swift
// LLMServiceProtocol.swift
protocol LLMServiceProtocol {
    func streamChat(
        messages: [ChatPayloadMessage],
        briefSnapshot: DesignBriefSnapshot?,
        currentStage: ProgressStageSnapshot?
    ) -> AsyncThrowingStream<String, Error>
}

// StructuredExtractorProtocol.swift
protocol StructuredExtractorProtocol {
    func extract(
        from messages: [ChatPayloadMessage],
        existing: DesignBriefSnapshot?
    ) async throws -> ExtractedFields
}
```

> **v0.1 简化：** ProgressAnalyzer 不定义 Protocol，直接用 struct。v0.2 如果需要 Mock 测试再提取协议。

### 2. MockLLMService — 苏格拉底式追问引擎

```swift
final class MockLLMService: LLMServiceProtocol {
    func streamChat(
        messages: [ChatPayloadMessage],
        briefSnapshot: DesignBriefSnapshot?,
        currentStage: ProgressStageSnapshot?
    ) -> AsyncThrowingStream<String, Error> {
        AsyncThrowingStream { continuation in
            Task {
                let stageOrder = currentStage?.order ?? 1

                // 只计算 user 消息数（排除 system/assistant）
                let userMessageCount = messages.filter { $0.role == "user" }.count

                let response = Self.pickResponse(
                    stageOrder: stageOrder,
                    userTurnCount: userMessageCount
                )

                // 模拟流式输出（每个字符 30ms）
                for char in response {
                    try? await Task.sleep(for: .milliseconds(30))
                    continuation.yield(String(char))
                }
                continuation.finish()
            }
        }
    }

    static let stageResponses: [Int: [String]] = [
        1: [
            "这个想法很有意思！先别急着展开——能告诉我，你说的这个问题，最常发生在哪类人身上吗？",
            "好的，你已经有了目标用户的大致画像。现在请闭上眼睛想象：这个人在什么时刻、什么地点，第一次意识到「我需要解决这个问题」？",
            "你描述了场景，但我还想知道：如果不解决这个痛点，最坏的结果是什么？用户会用什么笨办法凑合过去？",
            "到目前为止，你对痛点的描述还比较笼统。如果让你用一句话——只有一句话——向一个完全不了解背景的人解释这个问题，你会怎么说？",
        ],
        2: [
            "在你想到的领域里，目前有没有类似的产品或方案？它们做得好的地方是什么？",
            "那些方案没有解决的部分是什么？你的切入点和它们有什么本质区别？",
            "如果用户只能记住你这个项目的一个特点——一个——你希望是什么？",
        ],
        3: [
            "你列了不少功能。但如果只保留三个，其他全砍掉，哪三个必须留下？",
            "现在想想：有什么是你很想做、但 V1 阶段不应该做的？明确说「不做什么」和说「做什么」一样重要。",
            "你划定了一个边界。我想追问：排除的那些功能，是因为技术上做不到，还是因为它们不是核心痛点的直接解法？",
        ],
        4: [
            "你提到了几个核心功能。能拆开说说，每个功能背后需要什么技术支撑吗？",
            "这些技术模块里，哪个是你最没把握的？如果那个模块失败了，整个方案还能跑吗？",
            "想象一个用户从打开你的产品到完成任务，他会经历哪些步骤？",
        ],
        5: [
            "如果用户操作到一半出了错，你的系统应该怎么应对？直接报错？还是静默恢复？",
            "有没有一些规则是绝对不能打破的？比如数据隐私、安全限制？",
        ],
        6: [
            "你有多少时间完成这个项目？预算呢？这些约束会如何影响你的技术选型？",
            "有没有什么硬件或平台限制是你必须面对的？",
        ],
        7: [
            "你怎么知道这个产品「做成了」？能不能给我一个可以量化的数字？",
            "如果只能用两个指标来衡量成功，你会选哪两个？",
        ],
        8: [
            "你觉得这个项目最可能失败的地方在哪里？是技术风险、用户不买账、还是时间不够？",
            "针对你提到的最大风险，如果它真的发生了，你的 Plan B 是什么？",
        ],
        9: [
            "如果把这个项目拆成几个阶段，你会怎么划分里程碑？",
            "第一个里程碑应该验证什么？你打算花多少时间？",
        ],
    ]

    /// 根据阶段和用户轮次选择回复
    /// - Parameters:
    ///   - stageOrder: 当前活跃阶段序号 (1~9)
    ///   - userTurnCount: 用户已发送的消息数（不含 system/assistant）
    static func pickResponse(stageOrder: Int, userTurnCount: Int) -> String {
        let responses = stageResponses[stageOrder] ?? stageResponses[1]!
        // 用 userTurnCount 做索引，确保按轮次递进
        let index = min(userTurnCount % responses.count, responses.count - 1)
        return responses[index]
    }
}
```

> **v0.1 修正：** 之前用 `messages.count` 做索引，但 messages 数组包含 system/user/assistant 消息，会导致索引计算不准。改为只计算 `userTurnCount`。

### 3. MockStructuredExtractor — 关键词匹配提取器

```swift
final class MockStructuredExtractor: StructuredExtractorProtocol {
    func extract(
        from messages: [ChatPayloadMessage],
        existing: DesignBriefSnapshot?
    ) async throws -> ExtractedFields {
        try await Task.sleep(for: .milliseconds(300))

        guard let lastUserMessage = messages.last(where: { $0.role == "user" }) else {
            return ExtractedFields()
        }

        let text = lastUserMessage.content
        var fields = ExtractedFields()

        // ── 阶段 1：痛点与场景 ──
        let userKeywords = ["大学生", "新生", "学生", "老人", "儿童", "白领", "上班族",
                            "设计师", "开发者", "老师", "用户"]
        for keyword in userKeywords where text.contains(keyword) {
            fields.targetUser = extractContext(around: keyword, in: text)
            break
        }

        let painKeywords = ["找不到", "困难", "麻烦", "太慢", "复杂", "不会", "不懂",
                           "不方便", "效率低", "容易出错"]
        for keyword in painKeywords where text.contains(keyword) {
            fields.painPoint = extractContext(around: keyword, in: text)
            break
        }

        let scenarioKeywords = ["在", "当", "时候", "每天", "上课", "考试", "开会",
                               "出门", "回家"]
        for keyword in scenarioKeywords where text.contains(keyword) {
            fields.useScenario = extractContext(around: keyword, in: text)
            break
        }

        // ── 阶段 2：差异化价值 ──
        let valueKeywords = ["不同于", "比", "优势", "独特", "核心", "区别"]
        for keyword in valueKeywords where text.contains(keyword) {
            fields.differentiation = extractContext(around: keyword, in: text)
            break
        }

        // ── 阶段 3：边界 ──
        if text.contains("不做") || text.contains("排除") || text.contains("不考虑") {
            fields.boundaryItems = [
                BoundaryItemDTO(content: "从对话中提取的排除项", isIncluded: false)
            ]
        }

        return fields
    }

    private func extractContext(around keyword: String, in text: String) -> String {
        guard let range = text.range(of: keyword) else { return keyword }
        let center = text.distance(from: text.startIndex, to: range.lowerBound)
        let start = max(0, center - 10)
        let end = min(text.count, center + keyword.count + 20)
        let startIdx = text.index(text.startIndex, offsetBy: start)
        let endIdx = text.index(text.startIndex, offsetBy: end)
        return String(text[startIdx..<endIdx]).trimmingCharacters(in: .whitespaces)
    }
}
```

### 4. StageDefinition + BriefField（与 v3 一致）

```swift
struct StageDefinition {
    let order: Int
    let name: String
    let briefFields: [BriefField]
    let description: String

    func completionRatio(from snapshot: DesignBriefSnapshot) -> Double {
        guard !briefFields.isEmpty else { return 0 }
        let filled = briefFields.filter { $0.isFilled(in: snapshot) }.count
        return Double(filled) / Double(briefFields.count)
    }

    static let all: [StageDefinition] = [
        .init(order: 1, name: "痛点与场景锚定",
              briefFields: [.targetUser, .painPoint, .useScenario],
              description: "明确目标用户是谁，他们遇到什么具体问题，在什么场景下发生"),
        .init(order: 2, name: "差异化价值提炼",
              briefFields: [.coreValue, .differentiation],
              description: "提炼核心价值主张，明确与已有方案的差异"),
        .init(order: 3, name: "项目边界划定",
              briefFields: [.boundaryItems],
              description: "明确 MVP 做什么、不做什么，划定项目边界"),
        .init(order: 4, name: "功能与技术方案拆解",
              briefFields: [.mvpFeatures, .technicalModules, .interactionFlow],
              description: "拆解核心功能模块、技术选型和交互流程"),
        .init(order: 5, name: "运行逻辑与规则定义",
              briefFields: [.operationLogic],
              description: "定义系统运行逻辑、异常处理和规则约束"),
        .init(order: 6, name: "硬性约束设计",
              briefFields: [.hardConstraints],
              description: "明确预算、时间、硬件等不可突破的约束"),
        .init(order: 7, name: "量化验收标准制定",
              briefFields: [.successMetrics],
              description: "制定可量化的验收指标和目标值"),
        .init(order: 8, name: "风险识别与预案制定",
              briefFields: [.risks],
              description: "识别主要风险，制定缓解预案"),
        .init(order: 9, name: "项目阶段拆分与排期",
              briefFields: [.milestones],
              description: "拆分开发阶段，制定里程碑与排期"),
    ]
}

enum BriefField: String, CaseIterable {
    case targetUser, painPoint, useScenario
    case coreValue, differentiation
    case boundaryItems
    case mvpFeatures, technicalModules, interactionFlow
    case operationLogic, hardConstraints
    case successMetrics, risks, milestones

    func isFilled(in snapshot: DesignBriefSnapshot) -> Bool {
        switch self {
        case .targetUser:      return snapshot.targetUser?.isEmpty == false
        case .painPoint:       return snapshot.painPoint?.isEmpty == false
        case .useScenario:     return snapshot.useScenario?.isEmpty == false
        case .coreValue:       return snapshot.coreValue?.isEmpty == false
        case .differentiation: return snapshot.differentiation?.isEmpty == false
        case .boundaryItems:   return !snapshot.boundaryItems.isEmpty
        case .mvpFeatures:     return snapshot.mvpFeatures?.isEmpty == false
        case .technicalModules: return snapshot.technicalModules?.isEmpty == false
        case .interactionFlow: return snapshot.interactionFlow?.isEmpty == false
        case .operationLogic:  return snapshot.operationLogic?.isEmpty == false
        case .hardConstraints: return snapshot.hardConstraints?.isEmpty == false
        case .successMetrics:  return !snapshot.successMetrics.isEmpty
        case .risks:           return !snapshot.risks.isEmpty
        case .milestones:      return snapshot.milestones?.isEmpty == false
        }
    }
}
```

### 5. ProgressAnalyzer（v0.1：直接用 struct，无 Protocol）

```swift
struct ProgressAnalyzer {
    /// 根据 DesignBriefSnapshot 字段填充情况，计算各阶段新状态
    func analyze(
        brief: DesignBriefSnapshot,
        stages: [ProgressStageSnapshot]
    ) -> [ProgressStageSnapshot] {
        stages.map { stage in
            guard let def = StageDefinition.all.first(where: { $0.order == stage.order })
            else { return stage }

            let ratio = def.completionRatio(from: brief)
            var newStatus = stage.status

            switch ratio {
            case 0:
                newStatus = (stage.status == .notStarted) ? .notStarted : .active
            case 1.0:
                if stage.status != .needsReview { newStatus = .completed }
            default:
                newStatus = .active
            }

            return ProgressStageSnapshot(
                order: stage.order, name: stage.name,
                status: newStatus, completionRatio: ratio
            )
        }
    }

    /// 返回最关键的缺失字段（面向用户的自然语言描述）
    func missingFields(brief: DesignBriefSnapshot) -> [String] {
        for def in StageDefinition.all {
            let unfilled = def.briefFields.filter { !$0.isFilled(in: brief) }
            if !unfilled.isEmpty {
                return unfilled.map { field in
                    switch field {
                    case .targetUser:      return "目标用户画像"
                    case .painPoint:       return "核心痛点描述"
                    case .useScenario:     return "真实使用场景"
                    case .coreValue:       return "核心价值主张"
                    case .differentiation: return "差异化分析"
                    case .boundaryItems:   return "项目边界（做什么/不做什么）"
                    case .mvpFeatures:     return "MVP 功能列表"
                    case .technicalModules: return "技术模块拆解"
                    case .interactionFlow: return "交互流程"
                    case .operationLogic:  return "运行逻辑"
                    case .hardConstraints: return "硬性约束"
                    case .successMetrics:  return "量化验收标准"
                    case .risks:           return "风险与预案"
                    case .milestones:      return "里程碑排期"
                    }
                }
            }
        }
        return []
    }

    /// v0.1：检测 3 种核心认知动作
    func detectLearningTraces(
        previousBrief: DesignBriefSnapshot,
        currentBrief: DesignBriefSnapshot,
        activeStageOrder: Int
    ) -> [LearningTraceDTO] {
        var traces: [LearningTraceDTO] = []

        // reframe：用户修改了痛点或目标用户（且新值非空）
        let targetChanged = previousBrief.targetUser != currentBrief.targetUser
            && currentBrief.targetUser != nil
        let painChanged = previousBrief.painPoint != currentBrief.painPoint
            && currentBrief.painPoint != nil
        if targetChanged || painChanged {
            traces.append(LearningTraceDTO(
                stageOrder: activeStageOrder,
                actionType: "reframe",
                title: "你重新定义了问题",
                detail: "你修正了对目标用户或痛点的理解——这是设计思维中最关键的认知动作。好的设计往往从重新定义问题开始。"
            ))
        }

        // converge：某阶段从未填充变为有填充（首次收敛）
        for def in StageDefinition.all {
            let prevRatio = def.completionRatio(from: previousBrief)
            let currRatio = def.completionRatio(from: currentBrief)
            if prevRatio == 0 && currRatio > 0 {
                traces.append(LearningTraceDTO(
                    stageOrder: def.order,
                    actionType: "converge",
                    title: "你完成了一次收敛思考",
                    detail: "在「\(def.name)」阶段，你从模糊的想法收敛到了具体的描述。这就是设计思维中的「收敛」——把发散的信息聚焦为明确的判断。"
                ))
            }
        }

        // boundaryShrink：新增排除项
        let prevExcluded = previousBrief.boundaryItems.filter { !$0.isIncluded }.count
        let currExcluded = currentBrief.boundaryItems.filter { !$0.isIncluded }.count
        if currExcluded > prevExcluded {
            traces.append(LearningTraceDTO(
                stageOrder: activeStageOrder,
                actionType: "boundaryShrink",
                title: "你收缩了项目边界",
                detail: "你主动说了「不做什么」——这比说「做什么」更需要判断力。边界收缩是项目从空想走向落地的关键一步。"
            ))
        }

        return traces
    }
}
```

### 6. LiveLLMService / LiveStructuredExtractor — v0.1 骨架

```swift
// v0.1：Live 实现内部降级到 Mock，接口预留
// v0.2：替换为真实 API 调用

final class LiveLLMService: LLMServiceProtocol {
    private let fallback = MockLLMService()

    func streamChat(
        messages: [ChatPayloadMessage],
        briefSnapshot: DesignBriefSnapshot?,
        currentStage: ProgressStageSnapshot?
    ) -> AsyncThrowingStream<String, Error> {
        // v0.2 TODO:
        // 1. 读取 API Key（ProcessInfo / Keychain）
        // 2. 构造 HTTP request（messages, model, stream: true）
        // 3. URLSession.bytes(for:) 发起请求
        // 4. 逐行解析 SSE data，yield content delta
        // 5. 错误处理：apiKeyMissing / networkFailed / rateLimited
        return fallback.streamChat(
            messages: messages,
            briefSnapshot: briefSnapshot,
            currentStage: currentStage
        )
    }
}

final class LiveStructuredExtractor: StructuredExtractorProtocol {
    private let fallback = MockStructuredExtractor()

    func extract(
        from messages: [ChatPayloadMessage],
        existing: DesignBriefSnapshot?
    ) async throws -> ExtractedFields {
        // v0.2 TODO:
        // 1. 构造提取 Prompt（含 existing brief 和最近对话）
        // 2. 调用 LLM（非流式）
        // 3. 解析 JSON → ExtractedFields
        // 4. JSON 解析失败时静默跳过
        return try await fallback.extract(from: messages, existing: existing)
    }
}
```

---

## 八、Model ↔ DTO 转换（ModelExtensions.swift）

```swift
import SwiftData

// ── ChatMessage → DTO ──
extension ChatMessage {
    func toPayload() -> ChatPayloadMessage {
        ChatPayloadMessage(role: role, content: content)
    }
}

// ── DesignBrief → Snapshot ──
extension DesignBrief {
    func toSnapshot() -> DesignBriefSnapshot {
        DesignBriefSnapshot(
            targetUser: targetUser,
            painPoint: painPoint,
            useScenario: useScenario,
            coreValue: coreValue,
            differentiation: differentiation,
            boundaryItems: boundaryItems.map { $0.toDTO() },
            mvpFeatures: mvpFeatures,
            technicalModules: technicalModules,
            interactionFlow: interactionFlow,
            operationLogic: operationLogic,
            hardConstraints: hardConstraints,
            successMetrics: successMetrics.map { $0.toDTO() },
            risks: risks.map { $0.toDTO() },
            milestones: milestones
        )
    }
}

// ── ProgressStage → Snapshot ──
extension ProgressStage {
    func toSnapshot() -> ProgressStageSnapshot {
        ProgressStageSnapshot(
            order: order,
            name: name,
            status: stageStatusValue,
            completionRatio: completionRatio
        )
    }
}

// ── 子实体 → DTO ──
extension BoundaryItem {
    func toDTO() -> BoundaryItemDTO {
        BoundaryItemDTO(id: id, content: content, isIncluded: isIncluded)
    }
}

extension RiskItem {
    func toDTO() -> RiskItemDTO {
        RiskItemDTO(id: id, description: description,
                    probability: probability, impact: impact,
                    mitigation: mitigation)
    }
}

extension SuccessMetric {
    func toDTO() -> SuccessMetricDTO {
        SuccessMetricDTO(id: id, metric: metric,
                         target: target, measurement: measurement)
    }
}

extension LearningTrace {
    func toDTO() -> LearningTraceDTO {
        LearningTraceDTO(id: id, stageOrder: stageOrder,
                         actionType: actionType, title: title,
                         detail: detail, timestamp: timestamp)
    }
}

// ── 反向写入：ExtractedFields → DesignBrief（增量合并） ──
extension DesignBrief {
    func applyExtracted(_ fields: ExtractedFields, context: ModelContext) {
        // 简单字段：非 nil 才覆盖
        if let v = fields.targetUser { targetUser = v }
        if let v = fields.painPoint { painPoint = v }
        if let v = fields.useScenario { useScenario = v }
        if let v = fields.coreValue { coreValue = v }
        if let v = fields.differentiation { differentiation = v }
        if let v = fields.mvpFeatures { mvpFeatures = v }
        if let v = fields.technicalModules { technicalModules = v }
        if let v = fields.interactionFlow { interactionFlow = v }
        if let v = fields.operationLogic { operationLogic = v }
        if let v = fields.hardConstraints { hardConstraints = v }
        if let v = fields.milestones { milestones = v }

        // boundaryItems：替换式更新
        if let items = fields.boundaryItems {
            boundaryItems.forEach { context.delete($0) }
            boundaryItems = items.map {
                let bi = BoundaryItem(content: $0.content, isIncluded: $0.isIncluded)
                context.insert(bi)
                return bi
            }
        }

        // risks：替换式更新
        if let items = fields.risks {
            risks.forEach { context.delete($0) }
            risks = items.map {
                let ri = RiskItem(description: $0.description,
                                  probability: $0.probability,
                                  impact: $0.impact,
                                  mitigation: $0.mitigation)
                context.insert(ri)
                return ri
            }
        }

        // successMetrics：替换式更新
        if let items = fields.successMetrics {
            successMetrics.forEach { context.delete($0) }
            successMetrics = items.map {
                let sm = SuccessMetric(metric: $0.metric,
                                       target: $0.target,
                                       measurement: $0.measurement)
                context.insert(sm)
                return sm
            }
        }

        lastExtractedAt = Date()
    }
}
```

---

## 九、ChatViewModel 核心数据流

> **v0.1 线程约束：** ChatViewModel 标记 `@MainActor`，确保所有 SwiftData 访问在主线程。`sendMessage` 是 `@MainActor` 上的 async 方法，UI 更新和 SwiftData 操作都在主线程安全执行。

```swift
import SwiftData
import Observation

@MainActor
@Observable
final class ChatViewModel {
    let project: Project
    private let llmService: LLMServiceProtocol
    private let extractor: StructuredExtractorProtocol
    private let analyzer = ProgressAnalyzer()

    var currentStreamingText: String = ""
    var isStreaming: Bool = false
    var errorMessage: String?

    init(project: Project,
         llmService: LLMServiceProtocol,
         extractor: StructuredExtractorProtocol) {
        self.project = project
        self.llmService = llmService
        self.extractor = extractor
    }

    func sendMessage(_ text: String) async {
        guard !text.trimmingCharacters(in: .whitespaces).isEmpty else { return }
        guard let context = project.modelContext else { return }

        // ① 保存用户消息
        let userMsg = ChatMessage(role: "user", content: text)
        project.messages.append(userMsg)
        project.updatedAt = Date()

        // ② 快照当前状态（用于后续 diff）
        let previousBrief = project.brief?.toSnapshot() ?? DesignBriefSnapshot()

        // ③ 构造 DTO
        let payloadMessages = project.messages
            .sorted { $0.timestamp < $1.timestamp }
            .map { $0.toPayload() }
        let briefSnapshot = project.brief?.toSnapshot()
        let sortedStages = project.stages.sorted { $0.order < $1.order }
        let activeStage = sortedStages.first {
            $0.stageStatusValue == .active || $0.stageStatusValue == .notStarted
        }
        let stageSnapshot = activeStage?.toSnapshot()

        // ④ 流式调用 LLM
        isStreaming = true
        currentStreamingText = ""
        errorMessage = nil

        do {
            let stream = llmService.streamChat(
                messages: payloadMessages,
                briefSnapshot: briefSnapshot,
                currentStage: stageSnapshot
            )
            for try await token in stream {
                currentStreamingText += token
            }
        } catch {
            // v0.1: 简单错误提示（v0.2 升级为 ErrorBanner + 降级）
            errorMessage = "回复生成失败，请重试"
            isStreaming = false
            return
        }

        // ⑤ 保存 AI 回复
        let assistantMsg = ChatMessage(role: "assistant", content: currentStreamingText)
        project.messages.append(assistantMsg)
        currentStreamingText = ""
        isStreaming = false

        // ⑥ 结构化提取（失败不中断对话）
        // 注意：payloadMessages 已包含当前用户消息（步骤①已 append），无需再次追加
        do {
            let extracted = try await extractor.extract(
                from: payloadMessages,
                existing: briefSnapshot
            )
            if let brief = project.brief {
                brief.applyExtracted(extracted, context: context)
            }
        } catch {
            // v0.1: 提取失败静默处理
        }

        // ⑦ 进度分析
        let currentBriefSnapshot = project.brief?.toSnapshot() ?? DesignBriefSnapshot()
        let stageSnapshots = sortedStages.map { $0.toSnapshot() }
        let updatedStages = analyzer.analyze(
            brief: currentBriefSnapshot,
            stages: stageSnapshots
        )
        for (i, updated) in updatedStages.enumerated() {
            if i < sortedStages.count {
                let stage = sortedStages[i]
                stage.status = updated.status.rawValue
                stage.completionRatio = updated.completionRatio
                stage.lastUpdated = Date()
            }
        }

        // ⑧ 学习轨迹检测
        let traces = analyzer.detectLearningTraces(
            previousBrief: previousBrief,
            currentBrief: currentBriefSnapshot,
            activeStageOrder: activeStage?.order ?? 1
        )
        for traceDTO in traces {
            let trace = LearningTrace(
                stageOrder: traceDTO.stageOrder,
                actionType: traceDTO.actionType,
                title: traceDTO.title,
                detail: traceDTO.detail
            )
            project.learningTraces.append(trace)
        }

        // ⑨ 保存
        try? context.save()
    }
}
```

> **v0.1 修正点：**
> 1. 添加 `@MainActor` 注解，确保 SwiftData 操作线程安全
> 2. `guard let context = project.modelContext` 安全解包（modelContext 是 Optional）
> 3. `sortedStages` 只排序一次，后续复用（避免多次排序导致顺序不一致）
> 4. 提取时直接使用 `payloadMessages`（步骤①已 append 用户消息，无需再次追加）

---

## 十、MockDataFactory / SeedDataFactory

### MockDataFactory

```swift
import SwiftData

struct MockDataFactory {
    /// 生成完整示例项目，用于课程演示、SwiftUI Preview、调试
    static func createDemoProject(context: ModelContext) -> Project {
        let project = Project(
            name: "智能校园导航助手",
            briefDescription: "帮助大学新生在复杂校园中快速找到目的地的智能导航应用"
        )
        context.insert(project)

        // ── DesignBrief（部分字段已填充） ──
        let brief = DesignBrief(
            targetUser: "大一新生，尤其是来自外地的学生",
            painPoint: "校园面积大、建筑命名混乱，新生经常找不到教室和办公室",
            useScenario: "开学第一周，新生需要在 10 分钟内从宿舍赶到陌生的教学楼",
            coreValue: "基于 AR 的室内导航，解决 GPS 在建筑内失灵的问题",
            differentiation: "不同于百度/高德地图，专注室内场景 + 校园 POI 数据",
            boundaryItems: [
                BoundaryItem(content: "AR 实时导航箭头", isIncluded: true),
                BoundaryItem(content: "校园 POI 搜索", isIncluded: true),
                BoundaryItem(content: "课表导入与自动导航", isIncluded: true),
                BoundaryItem(content: "社交功能（找同学）", isIncluded: false),
                BoundaryItem(content: "外卖配送", isIncluded: false),
            ],
            mvpFeatures: "AR 导航 + POI 搜索 + 课表导入",
            technicalModules: "ARKit + CoreLocation + 本地 SQLite POI 数据库",
            successMetrics: [
                SuccessMetric(metric: "首次导航成功率", target: "≥ 90%"),
                SuccessMetric(metric: "平均找到目的地时间", target: "≤ 5 分钟",
                              measurement: "从发起导航到到达"),
            ],
            risks: [
                RiskItem(description: "AR 在弱光环境下识别不稳定",
                         probability: 4, impact: 4,
                         mitigation: "增加 2D 地图备选方案"),
                RiskItem(description: "POI 数据采集工作量大",
                         probability: 3, impact: 5,
                         mitigation: "先覆盖主楼，后续众包"),
            ]
        )
        // 关键：必须 insert 到 context，再赋值给 project.brief
        context.insert(brief)
        project.brief = brief

        // ── 9 个 ProgressStage ──
        let stages = StageDefinition.all.map { def in
            ProgressStage(order: def.order, name: def.name)
        }
        project.stages = stages

        // 根据 brief 填充情况设置状态
        stages[0].status = "completed"; stages[0].completionRatio = 1.0   // 痛点与场景
        stages[1].status = "completed"; stages[1].completionRatio = 1.0   // 差异化价值
        stages[2].status = "completed"; stages[2].completionRatio = 1.0   // 项目边界
        stages[3].status = "active";    stages[3].completionRatio = 0.67  // 功能与技术
        // stages[4]~[8] 保持 notStarted（默认值）

        // ── ChatMessage（模拟多轮对话） ──
        let conversation: [(String, String)] = [
            ("user", "我想做一个帮助大学生在校园里导航的应用"),
            ("assistant", "这个想法很实际！能具体说说，你觉得哪类学生最需要这个？"),
            ("user", "主要是大一新生，尤其是外地来的，对校园完全不熟悉"),
            ("assistant", "你能描述一个具体的场景吗？比如这个新生在什么情况下最着急找不到路？"),
            ("user", "开学第一周，在宿舍出发去一个完全没去过的教学楼上课，快迟到了"),
            ("assistant", "很好的场景描述。那在你想到的领域里，目前有没有类似的产品？"),
            ("user", "高德百度地图都有导航，但在建筑里面 GPS 就失灵了"),
            ("assistant", "你发现了一个关键差异点。那你觉得你的方案和这些地图应用有什么本质区别？"),
        ]
        for (i, (role, content)) in conversation.enumerated() {
            let msg = ChatMessage(
                role: role, content: content,
                timestamp: Date().addingTimeInterval(Double(i) * 60)
            )
            project.messages.append(msg)
        }

        // ── LearningTrace ──
        let traces: [(Int, String, String, String)] = [
            (1, "converge", "你完成了一次收敛思考",
             "从模糊的「校园应用」收敛到了「导航」这个具体问题"),
            (1, "reframe", "你重新定义了问题",
             "从「校园信息应用」重新定义为「室内导航」问题"),
            (3, "boundaryShrink", "你收缩了项目边界",
             "排除了社交功能和外卖配送，聚焦核心导航需求"),
        ]
        for (order, type, title, detail) in traces {
            let trace = LearningTrace(
                stageOrder: order, actionType: type,
                title: title, detail: detail
            )
            project.learningTraces.append(trace)
        }

        return project
    }
}
```

> **v0.1 修正：** 之前遗漏了 `context.insert(brief)` 和 `project.brief = brief`。DesignBrief 作为 `@Model` 必须被 insert 到 ModelContext 才能被 SwiftData 追踪。赋值 `project.brief = brief` 建立双向关系。子实体（BoundaryItem、RiskItem、SuccessMetric）通过 DesignBrief 的 Relationship cascade 自动管理，无需手动 insert。

### SeedDataFactory

```swift
import SwiftData

struct SeedDataFactory {
    static func seedIfNeeded(context: ModelContext) {
        let descriptor = FetchDescriptor<Project>()
        let count = (try? context.fetchCount(descriptor)) ?? 0
        guard count == 0 else { return }

        _ = MockDataFactory.createDemoProject(context: context)
        try? context.save()
    }
}
```

---

## 十一、InsightsPanel 设计

v0.1 不使用独立 ViewModel，直接读取 Project 数据渲染。

```swift
struct InsightsPanel: View {
    let project: Project

    var body: some View {
        ScrollView {
            VStack(spacing: 20) {
                // ── Section 1: 设计简报摘要 ──
                BriefSummarySection(brief: project.brief)

                Divider()

                // ── Section 2: 学习轨迹时间线 ──
                LearningTraceTimeline(
                    traces: project.learningTraces.sorted { $0.timestamp < $1.timestamp }
                )
            }
            .padding()
        }
    }
}

/// 设计简报摘要：按阶段分组，显示已填充/未填充字段
struct BriefSummarySection: View {
    let brief: DesignBrief?

    var body: some View {
        VStack(alignment: .leading, spacing: 16) {
            Text("设计简报")
                .font(.title2).bold()

            if let brief {
                let snapshot = brief.toSnapshot()
                ForEach(StageDefinition.all, id: \.order) { def in
                    VStack(alignment: .leading, spacing: 4) {
                        Text(def.name)
                            .font(.subheadline).bold()
                        ForEach(def.briefFields, id: \.rawValue) { field in
                            HStack {
                                Image(systemName: field.isFilled(in: snapshot)
                                      ? "checkmark.circle.fill" : "circle")
                                    .foregroundStyle(field.isFilled(in: snapshot)
                                                     ? .green : .gray)
                                Text(displayName(for: field))
                                    .font(.caption)
                                    .foregroundStyle(field.isFilled(in: snapshot)
                                                     ? .primary : .secondary)
                            }
                        }
                    }
                }
            } else {
                Text("暂无设计简报数据")
                    .foregroundStyle(.secondary)
            }
        }
    }

    private func displayName(for field: BriefField) -> String {
        switch field {
        case .targetUser:      return "目标用户"
        case .painPoint:       return "核心痛点"
        case .useScenario:     return "使用场景"
        case .coreValue:       return "核心价值"
        case .differentiation: return "差异化"
        case .boundaryItems:   return "项目边界"
        case .mvpFeatures:     return "MVP 功能"
        case .technicalModules: return "技术模块"
        case .interactionFlow: return "交互流程"
        case .operationLogic:  return "运行逻辑"
        case .hardConstraints: return "硬性约束"
        case .successMetrics:  return "验收标准"
        case .risks:           return "风险预案"
        case .milestones:      return "里程碑"
        }
    }
}

/// 学习轨迹时间线：纵向排列 ReflectionCard
struct LearningTraceTimeline: View {
    let traces: [LearningTrace]

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("学习轨迹")
                .font(.title2).bold()

            if traces.isEmpty {
                Text("还没有学习轨迹——开始和 AI 对话，你的设计思维动作会被记录在这里")
                    .foregroundStyle(.secondary)
                    .font(.caption)
            } else {
                ForEach(traces) { trace in
                    ReflectionCard(trace: trace)
                }
            }
        }
    }
}
```

---

## 十二、v0.1 开发顺序

### 核心原则

1. **每完成一步，Xcode 都能编译运行**
2. **第一批完成后，App 打开就能看到 demo 数据**
3. **第二批 Step 21 完成后，核心对话闭环跑通**

### 第一批：数据层 + 服务骨架（Day 1~3）

| Step | 文件 | 验收标准 |
|------|------|---------|
| **1** | Models/ 全部 8 个 @Model（含手写 init） | ✅ 编译通过，App 启动看到空列表 |
| **2** | DTOs/ 全部 8 个 struct（含显式 init + ChatPayloadMessage 工厂方法） | ✅ 编译通过 |
| **3** | Services/StageDefinition.swift + BriefField enum | ✅ 编译通过 |
| **4** | Models/ModelExtensions.swift（所有 toSnapshot/toDTO/toPayload + applyExtracted） | ✅ 编译通过 |
| **5** | Factories/MockDataFactory.swift + SeedDataFactory.swift | ✅ **App 启动看到 demo 项目** |
| **6** | Extensions/Color+Theme.swift | ✅ 编译通过 |
| **7** | Services/Protocols/ 两个协议 | ✅ 编译通过 |
| **8** | Services/Mock/MockLLMService.swift | ✅ 编译通过 |
| **9** | Services/Mock/MockStructuredExtractor.swift | ✅ 编译通过 |
| **10** | Services/Live/ 两个骨架文件 | ✅ 编译通过（降级到 Mock） |
| **11** | Services/ProgressAnalyzer.swift | ✅ 编译通过 |
| **12** | CoDesign_AgentApp.swift（EnvironmentKey 定义 + 服务注入 + SeedData 调用） | ✅ App 启动有 demo 项目 |

> **第一批结束时：** App 能启动，列表中有 demo 项目。所有数据层和服务层就绪。

### 第二批：UI 层 + 闭环联调（Day 4~8）

| Step | 文件 | 验收标准 |
|------|------|---------|
| **13** | ViewModels/ProjectListViewModel.swift | ✅ 编译通过 |
| **14** | Views/ProjectList/ProjectListView.swift | ✅ **列表页展示 demo 项目，点击进入详情** |
| **15** | ViewModels/NewProjectViewModel.swift | ✅ 编译通过 |
| **16** | Views/NewProject/NewProjectView.swift | ✅ **能新建项目（自动创建 DesignBrief + 9 ProgressStage），返回列表可见** |
| **17** | ViewModels/ProjectDetailViewModel.swift | ✅ 编译通过 |
| **18** | Views/ProjectDetail/ProjectDetailView.swift | ✅ **详情页 Tab 切换正常（内容为空占位）** |
| **19** | Components/MessageBubble.swift + TypingIndicatorView.swift | ✅ 编译通过 |
| **20** | ViewModels/ChatViewModel.swift（@MainActor） | ✅ 编译通过 |
| **21** | Views/ProjectDetail/ChatPanel.swift | ✅ **核心里程碑：能发消息，看到 Mock AI 流式回复** |
| **22** | Components/StageNodeView.swift | ✅ 编译通过 |
| **23** | Views/ProjectDetail/ProgressPanel.swift | ✅ **9 步进度可视化，阶段状态随对话变化** |
| **24** | Components/ReflectionCard.swift | ✅ 编译通过 |
| **25** | Views/ProjectDetail/InsightsPanel.swift | ✅ **DesignBrief 摘要 + 学习轨迹时间线展示** |

> **第二批结束时：** 核心闭环完整可演示。

### 三人排期（8 个工作日）

| 天 | A（数据 + API） | B（AI 逻辑 + ViewModel） | C（界面） |
|----|----------------|------------------------|----------|
| **D1** | Step 1-4: Models + DTOs + StageDefinition + ModelExtensions | 审阅 v0.1 spec，准备 Mock 回复文案 | Color+Theme 设计 |
| **D2** | Step 5-6: MockDataFactory + SeedDataFactory | Step 7-8: Protocols + MockLLMService | Step 13-14: 列表页 |
| **D3** | Step 9-12: MockExtractor + Live 骨架 + ProgressAnalyzer + App 入口 | MockStructuredExtractor 调试 | Step 15-16: 新建页 |
| **D4** | 联调支持 | Step 17-18: 详情页骨架 | Step 17-18: 详情页骨架 |
| **D5** | 联调支持 | Step 19-20: ChatViewModel | Step 19+21: ChatPanel + 组件 |
| **D6** | Bug 修复 | Step 22-23: 提取 + 进度联调 | Step 22-23: 进度面板 |
| **D7** | Bug 修复 | Step 24-25: 学习轨迹 + 洞察联调 | Step 24-25: 反思卡片 + 洞察面板 |
| **D8** | 全面联调 | 全面联调 + demo 数据优化 | UI 打磨 |

### 里程碑检查点

| 时间 | 必须跑通的场景 | 验收方式 |
|------|---------------|---------|
| **D2 结束** | App 启动 → 列表中出现 demo 项目 | 模拟器运行 |
| **D4 结束** | 新建项目 → 列表中出现 → 进入详情页 → Tab 切换 | 手动操作 |
| **D5 结束** | 进入聊天 → 发消息 → Mock AI 流式回复出现 | 发 2 条消息观察 |
| **D7 结束** | 聊天 → 字段提取 → 进度节点变化 → 反思卡片出现 | 完整对话 5 轮 |
| **D8 结束** | 完整闭环：新建 → 对话 → 进度 → 洞察 → 学习轨迹 | 课程演示彩排 |

---

## 十三、v0.1 → v0.2 升级路径

| v0.2 功能 | 依赖的 v0.1 基础 | 工作量 |
|-----------|----------------|--------|
| LiveLLMService 真实 API | MockLLMService + Protocol 已跑通 | 1~2 天 |
| LiveStructuredExtractor | MockStructuredExtractor + Protocol 已跑通 | 1~2 天 |
| PromptTemplates 独立文件 | Live 骨架已预留位置 | 0.5 天 |
| ErrorBanner + 降级流程 | 服务注入机制 + EnvironmentKey 已就绪 | 1 天 |
| conversationSummary + ConversationContextBuilder | ChatViewModel 数据流已稳定 | 1 天 |
| ReportExporter + ExportPanel | InsightsPanel 已展示摘要 | 1~2 天 |
| RiskMatrixView 精细视图 | RiskItem 模型 + DTO 已存在 | 1 天 |
| BoundaryTableView 独立组件 | BoundaryItem 模型 + DTO 已存在 | 0.5 天 |
| 完整 7 种 actionType | 3 种核心已实现 | 0.5 天 |
| 回溯交互（点击已完成节点） | StageNodeView 已渲染 4 种状态 | 1 天 |
| @Model → Protocol 化 ProgressAnalyzer | struct 已实现，只需提取协议 | 0.5 天 |

---

## 十四、v0.1 相对 v3 的完整变化汇总

| 维度 | v3 | v0.1 |
|------|----|----|
| 文件总数 | ~55 | 42 |
| Model 数量 | 8 | 8（不变） |
| Model 字段 | 含 conversationSummary, lastSummaryAtMessageCount | 不含（v0.1 对话短） |
| DTO 数量 | 8 | 8（不变，但补充了显式 init） |
| Service 数量 | 11（含 PromptTemplates, ConversationContextBuilder, ReportExporter） | 8（去掉 3 个） |
| ProgressAnalyzer | 遵循 ProgressAnalyzerProtocol | 直接用 struct（v0.1 不定义 Protocol） |
| ViewModel 数量 | 5（含 InsightsViewModel） | 4（InsightsPanel 直接渲染） |
| View + Component 数量 | 13 | 8（去掉 ExportPanel, StageProgressBar, MissingFieldsCard, RiskMatrixView, BoundaryTableView, ErrorBanner, EmptyStateView 等） |
| Extensions | 3（Color, Date, View+Animations） | 1（仅 Color+Theme） |
| 认知动作类型 | 7 种 | 3 种（reframe, converge, boundaryShrink） |
| 错误处理 | 8 种 AppError + 降级矩阵 + ErrorBanner | errorMessage: String? + 简单提示 |
| 上下文管理 | 四层构造 + conversationSummary | 直接传全部消息（v0.1 对话短） |
| 并发 | ModelActor | @MainActor + 主线程（数据量小） |
| 导出 | Markdown + ShareLink | 无 |
| EnvironmentKey | 未明确定义 | 显式定义 LLMServiceKey + StructuredExtractorKey |
| PromptTemplates | 独立文件 | v0.1 不独立，Live 骨架内置占位 |
